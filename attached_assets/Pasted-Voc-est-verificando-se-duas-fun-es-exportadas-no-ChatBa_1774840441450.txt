Você está verificando se duas funções exportadas no ChatBanana realmente estão sem uso antes de decidir o que fazer com elas.

CONTEXTO: Uma auditoria identificou duas funções exportadas possivelmente sem uso externo:
- buildMessageContext (localização: servidor, arquivo exato a confirmar)
- getAllSessions (server/services/whatsappWebJS.ts)

TAREFA: Para cada função, fazer uma verificação completa e tomar a ação correta.

---

PASSO 1 — buildMessageContext:

1.1 Localize o arquivo exato onde buildMessageContext é definida e exportada.

1.2 Faça um grep por "buildMessageContext" em todo o projeto (exceto node_modules).

1.3 Com base no resultado:
  - Se for importada e usada em algum arquivo → NENHUMA AÇÃO. Informe onde é usada.
  - Se não for importada em nenhum lugar → verifique o corpo da função. Se for lógica genérica reutilizável (ex: monta contexto de mensagem para IA/automações), adicione um comentário JSDoc descritivo e mantenha:
    ```typescript
    /**
     * Monta o contexto de mensagem para uso em automações e respostas de IA.
     * Utilizado internamente pelo automationEngine e message-processor.
     * @internal
     */
    export function buildMessageContext(...) {
    ```
  - Se não for importada E for código morto óbvio (lógica trivial ou duplicada de outro lugar) → REMOVA e informe.

---

PASSO 2 — getAllSessions (whatsappWebJS.ts):

2.1 Faça um grep por "getAllSessions" em todo o projeto (exceto node_modules).

2.2 Com base no resultado:
  - Se for importada e usada em algum arquivo → NENHUMA AÇÃO. Informe onde é usada.
  - Se não for importada em nenhum lugar → verifique se faz sentido para um painel admin futuro (ex: retorna todas as sessões Baileys ativas). Se sim, adicione comentário JSDoc e mantenha:
    ```typescript
    /**
     * Retorna todas as sessões Baileys ativas no servidor.
     * Reservado para uso no painel super_admin.
     * @internal
     */
    export function getAllSessions() {
    ```
  - Se for código morto sem valor futuro claro → REMOVA e informe.

---

PASSO 3 — Adicionar cabeçalho de arquitetura em channel-router.ts:

O projeto mantém dois canais WhatsApp em paralelo de forma intencional. Para evitar confusão futura de quem ler o código, adicione um comentário de arquitetura no topo de server/services/channel-router.ts (logo após os imports):

```typescript
/**
 * ARQUITETURA DE CANAIS — ChatBanana
 *
 * Este roteador gerencia dois canais WhatsApp simultâneos:
 *
 * 1. META CLOUD API OFICIAL (meta-whatsapp.ts)
 *    - Números de WhatsApp Business verificados pela Meta
 *    - Conexão via whatsapp_connections table (Meta Embedded Signup)
 *    - Obrigatório para envio de templates HSM
 *
 * 2. BAILEYS / WHATSAPP WEB (whatsappWebJS.ts)
 *    - Números pessoais ou não-verificados via QR Code
 *    - Conexão via sessões locais (conexoes table)
 *    - NÃO é resquício do Z-API — é canal ativo e intencional
 *
 * O roteamento é feito por workspace: se o workspace tem uma
 * conexão Meta ativa, usa Meta. Caso contrário, usa Baileys.
 */
```

Adicione também no topo de server/services/whatsappWebJS.ts (após os imports):

```typescript
/**
 * Canal Baileys — WhatsApp Web via WebSocket
 *
 * Implementa conexão WhatsApp para números não-verificados via QR Code.
 * Funciona em paralelo com Meta Cloud API (channel-router.ts).
 * NÃO confundir com integrações legadas (Z-API foi removido do projeto).
 */
```

VALIDAÇÃO: Ao final, informe:
- Status de buildMessageContext (usada / mantida com JSDoc / removida)
- Status de getAllSessions (usada / mantida com JSDoc / removida)
- Confirmação de que os comentários de arquitetura foram adicionados