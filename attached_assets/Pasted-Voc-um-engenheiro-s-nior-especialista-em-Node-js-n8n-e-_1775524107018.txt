Você é um engenheiro sênior especialista em Node.js, n8n e arquitetura SaaS.

CONTEXTO:
Existe um node HTTP no n8n que envia dados para `/api/n8n/ai`.

O payload atual está FUNCIONANDO em produção.

IMPORTANTE:

* NÃO pode haver breaking changes
* NÃO pode remover campos existentes
* NÃO pode alterar nomes atuais
* NÃO pode mudar comportamento existente
* Apenas EXTENDER e ORGANIZAR o payload

---

OBJETIVO:

Melhorar o payload do agente financeiro tornando ele:

* mais previsível
* mais controlado
* mais eficiente para IA
* pronto para escala multi-tenant

---

IMPLEMENTAÇÕES OBRIGATÓRIAS:

1. MANTER ESTRUTURA ATUAL

Preservar:

* workspace_id
* prompt_name
* mensagem
* context
* historico

---

2. NÃO REMOVER Object.assign

IMPORTANTE:

* NÃO remover Object.assign({}, $json)
* Apenas complementar com um novo objeto estruturado

---

3. CRIAR BLOCO "contexto_controlado"

Adicionar dentro do context:

contexto_controlado: {

intent: $json.intent,
fluxo_atual: $json.fluxo_atual,
cliente_nome: $json.cliente_nome,
ja_identificado: $json.ja_identificado,

motivo: $json.motivo,
prioridade: $json.prioridade,

mudou_intencao: $json.mudou_intencao,
ambiguidade: $json.ambiguidade,

tipo_input: $json.tipo_input ?? "texto",

financeiro: {
valor_devido: $json.financial?.valor_devido ?? 0,
suspenso: $json.financial?.is_suspended ?? false,
faturas_abertas: $json.financial?.faturas_abertas ?? 0
},

conexao: {
status: $json.conexao?.status ?? "desconhecido"
}
}

---

4. ADICIONAR CONTROLE DE COMPORTAMENTO

Adicionar:

modo: {
respeitar_intencao: true,
ignorar_fluxo_anterior: $json.mudou_intencao ?? false
}

---

5. METADADOS OPERACIONAIS

Adicionar:

operacao: {
origem: "n8n",
canal: "whatsapp",
versao: "v1"
}

---

6. IDENTIFICAÇÃO DO INPUT

Adicionar:

tipo_input: $json.tipo_input ?? "texto"

---

7. CONTEXTO EXISTENTE (MANTER)

Preservar:

* conversation_id
* telefone
* contact_name
* sessao
* briefing

---

8. COMPATIBILIDADE TOTAL

Garantir:

* Nenhuma chave existente seja removida
* Object.assign continue funcionando
* Sistema funcione mesmo ignorando novos campos
* Código null-safe

---

9. RESULTADO FINAL (CONCEITO)

O context final deve conter:

{
...dados antigos,
contexto_controlado: {...},
modo: {...},
operacao: {...}
}

---

IMPORTANTE:

Se houver qualquer dúvida:

* NÃO simplificar
* NÃO remover campos
* NÃO refatorar estrutura existente

Apenas ORGANIZAR e ADICIONAR.

---

RESULTADO ESPERADO:

* IA mais estável
* menos erro de interpretação
* melhor desempenho
* pronto para múltiplos provedores
