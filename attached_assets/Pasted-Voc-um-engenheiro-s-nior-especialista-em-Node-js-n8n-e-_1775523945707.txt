Você é um engenheiro sênior especialista em Node.js, n8n e integrações HTTP.

CONTEXTO:
Existe um node HTTP no n8n responsável por enviar mensagens para o endpoint `/api/n8n/send`.

O payload atual já está FUNCIONANDO em produção e tem a seguinte estrutura:

{
conversation_id,
message,
humanize: true
}

IMPORTANTE:

* NÃO pode haver breaking changes
* NÃO pode remover campos existentes
* NÃO pode alterar nomes atuais
* NÃO pode mudar comportamento existente
* Apenas EXTENDER com melhorias

---

OBJETIVO:

Melhorar a robustez do envio de mensagens sem impactar o funcionamento atual.

---

IMPLEMENTAÇÕES OBRIGATÓRIAS:

1. PROTEÇÃO CONTRA RESPOSTA VAZIA

Garantir que:

* Se message for null, undefined, vazio ou inválido
* Substituir por mensagem padrão:

"Desculpa, tive um problema aqui 😕 Pode repetir pra mim?"

IMPORTANTE:

* NÃO alterar lógica existente
* Apenas envolver com validação

---

2. VALIDAÇÃO DE TIPO

Garantir que:

* message sempre seja string
* Caso contrário → converter ou usar fallback

---

3. ADICIONAR METADADOS (SEM QUEBRAR)

Adicionar novo objeto opcional "meta":

meta: {
intent: $json.intent ?? null,
agente: $json.prompt_name ?? null,
timestamp: new Date().toISOString()
}

IMPORTANTE:

* NÃO substituir nada existente
* Apenas adicionar

---

4. IDENTIFICAÇÃO DA RESPOSTA

Adicionar campos opcionais:

tipo: "texto"
origem: "ia"

---

5. ESTRUTURA FINAL (EXEMPLO)

O payload final deve ser equivalente a:

{
conversation_id,
message,
humanize: true,

tipo: "texto",
origem: "ia",

meta: {
intent,
agente,
timestamp
}
}

---

6. COMPATIBILIDADE

Garantir:

* Se backend ignorar novos campos → tudo continua funcionando
* Nenhuma chave existente seja alterada
* Nenhuma estrutura atual seja removida

---

7. CÓDIGO DEFENSIVO

Garantir:

* null safety em todos os campos
* fallback seguro
* nenhuma exceção em runtime

---

IMPORTANTE:

Se houver qualquer dúvida:

* NÃO simplificar
* NÃO refatorar
* NÃO remover código existente
* NÃO alterar comportamento atual

Apenas EXTENDER o payload com segurança.

---

RESULTADO ESPERADO:

* Evitar envio de mensagens vazias
* Melhor rastreabilidade (logs e métricas)
* Base pronta para analytics
* Sistema mais resiliente
